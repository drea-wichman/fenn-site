<div class="banner">
  <div class="banner__inner">
    <p>Fenn Agritech is a fictional company built for a GRC portfolio. <a class="banner__link" href="/building-fenn">Learn more →</a></p>
  </div>
</div>

<style>
  .banner {
    background: #0A0A0A;
    color: var(--color-paper);
    padding: 8px 0;
    text-align: center;
  }

  .banner__inner {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: 0 var(--gutter);
  }

  .banner p {
    font-family: var(--font-mono);
    font-size: 11px;
    font-weight: 400;
    letter-spacing: 0.04em;
    color: var(--color-paper);
  }

  .banner__link {
    color: var(--color-steel-light);
    font-weight: 500;
    text-decoration: none;
    text-underline-offset: 3px;
    text-decoration-thickness: 1px;
    cursor: pointer;
    padding: 0;
    transition: text-decoration-color var(--transition);
  }

  .banner__link:hover {
    text-decoration: underline;
  }
</style>
