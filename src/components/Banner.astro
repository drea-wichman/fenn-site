<div class="banner">
  <div class="banner__inner">
    <p>Fenn Agritech is a fictional company built for a GRC portfolio. <a class="banner__link" href="https://github.com/drea-wichman/fenn/blob/main/building-fenn.md" target="_blank" rel="noopener noreferrer">Learn more →</a></p>
  </div>
</div>

<style>
  .banner {
    background: var(--color-emerald);
    color: var(--text-on-dark);
    padding: 8px 0;
    text-align: center;
  }
  
  .banner__inner {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: 0 var(--gutter);
  }
  
  .banner p {
    font-size: 12px;
    font-weight: 500;
    color: var(--text-on-dark);
  }
  
  .banner__link {
    color: var(--text-on-dark);
    font-weight: 600;
    text-decoration: underline;
    text-underline-offset: 2px;
    cursor: pointer;
    padding: 0;
  }
  
  .banner__link:hover {
    opacity: 0.85;
  }
</style>