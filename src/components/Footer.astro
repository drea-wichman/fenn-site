<footer class="footer">
  <div class="footer__rule" aria-hidden="true"></div>
  <div class="footer__inner">
    <div class="footer__brand">
      <div class="footer__logo" aria-label="Fenn">
        <svg viewBox="-240 -165 825 170" xmlns="http://www.w3.org/2000/svg">
          <path d="M 35 -100 Q 35 -158 80 -158 L 110 -158 L 110 -151 L 82 -151 Q 42 -151 42 -100 L 42 0 L 35 0 Z" fill="currentColor"/>
          <rect x="-235" y="-104" width="815" height="7" fill="currentColor"/>
          <rect x="80" y="-95" width="7" height="95" fill="currentColor"/>
          <rect x="80" y="-54" width="56" height="7" fill="currentColor"/>
          <rect x="80" y="-7" width="64" height="7" fill="currentColor"/>
          <path d="M 158 -80 L 165 -80 L 228 -15 L 228 -80 L 235 -80 L 235 0 L 228 0 L 165 -65 L 165 0 L 158 0 Z" fill="currentColor"/>
          <path d="M 253 -80 L 260 -80 L 323 -15 L 323 -80 L 330 -80 L 330 0 L 323 0 L 260 -65 L 260 0 L 253 0 Z" fill="currentColor"/>
        </svg>
      </div>
      <p class="footer__tagline">Fenn Agritech &middot; Bristol, England &middot; 2019</p>
    </div>

    <div class="footer__columns">
      <div class="footer__column">
        <h4 class="footer__heading">Company</h4>
        <ul>
          <li><a href="/trust-center">Trust Center</a></li>
          <li><a href="/behind-fenn">Behind Fenn</a></li>
        </ul>
      </div>

      <div class="footer__column">
        <h4 class="footer__heading">Legal</h4>
        <ul>
          <li><a href="/privacy">Privacy Policy</a></li>
          <li><a href="/terms">Terms of Service</a></li>
        </ul>
      </div>

      <div class="footer__column">
        <h4 class="footer__heading">Portfolio</h4>
        <ul>
          <li><a href="https://github.com/drea-wichman" target="_blank" rel="noopener">GitHub &rarr;</a></li>
          <li><a href="https://wichman.io" target="_blank" rel="noopener">wichman.io &rarr;</a></li>
        </ul>
      </div>
    </div>
  </div>
</footer>

<style>
  .footer {
    background: var(--color-ink);
    color: var(--color-paper);
    padding: 0 0 32px;
  }

  .footer__rule {
    height: 1px;
    background: var(--divider-dark);
    max-width: var(--max-width);
    margin: 0 auto 56px;
  }

  .footer__inner {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: 0 var(--gutter);
    display: grid;
    grid-template-columns: auto auto;
    justify-content: space-between;
    gap: 80px;
    margin-bottom: 60px;
  }

  .footer__logo {
    width: 200px;
    height: 52px;
    color: var(--color-paper);
  }

  .footer__logo svg {
    width: 100%;
    height: 100%;
  }

  .footer__tagline {
    font-family: var(--font-mono);
    font-size: 11px;
    color: var(--text-muted-on-dark);
    letter-spacing: 0.04em;
    margin: 20px 0 0;
    line-height: 1.6;
  }

  .footer__columns {
    display: grid;
    grid-template-columns: repeat(3, auto);
    gap: 64px;
  }

  .footer__heading {
    font-family: var(--font-mono);
    color: var(--color-steel-light);
    font-size: 10px;
    font-weight: 400;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    margin-bottom: 18px;
  }

  .footer__column ul {
    list-style: none;
  }

  .footer__column li {
    margin-bottom: 8px;
  }

  .footer__column a {
    font-family: var(--font-display);
    font-size: 15px;
    color: var(--color-paper);
    transition: color var(--transition);
  }

  .footer__column a:hover {
    color: var(--color-clay);
  }

  @media (max-width: 768px) {
    .footer__inner {
      grid-template-columns: 1fr;
      gap: 40px;
    }

    .footer__columns {
      grid-template-columns: repeat(2, 1fr);
      gap: 32px;
    }
  }
</style>
